spring.application.name=QuizLynx

#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.datasource.url =${SPRING_DATASOURCE_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USER}
#spring.jpa.show-sql=true
#spring.datasource.password=${SPRING_DATASOURCE_PASS}
spring.datasource.url=jdbc:postgresql://ep-autumn-band-ahlwk367-pooler.c-3.us-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_bl3wpCg7fAMv&sslmode=require&channelBinding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_bl3wpCg7fAMv
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgresPlusDialect
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=alpha
springdoc.swagger-ui.withCredentials=true



spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false


jwt.secret=${JWT_SECRET}

# OAuth2 client registration for Google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri=${GOOGLE_REDIRECT_URI}
spring.security.oauth2.client.registration.google.authorization-grant-type=${AUTH_GRANT_TYPE}
spring.security.oauth2.client.registration.google.client-authentication-method=${CLIENT_AUTH_METHOD}

# OAuth2 provider details for Google
spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://openidconnect.googleapis.com/v1/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=sub


# 4. Add these debugging configurations to your application.properties

# Enable debug logging for OAuth2 and Security
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.oauth2=DEBUG
logging.level.com.projectJava.quizApp.config=DEBUG
logging.level.org.springframework.web.cors=DEBUG



spring.mail.host=smtp.gmail.com
spring.mail.port=${SPRING_MAIL_PORT}
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASS}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=${SPRING_MAIL_USERNAME}

# Secret used to HMAC OTPs (store in env in prod)
otp.hmac.secret = ${HMAC_SECRET}

# OTP settings
otp.length = 6
otp.expiration.minutes = 10
otp.max.attempts = 5
otp.resend.cooldown.seconds = 60


server.port=8080
server.ssl.enabled=true
server.ssl.key-store=classpath:localhost-mkcert.p12
server.ssl.key-store-password=${KEY_STORE_PASS}
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=${SSL_KEY_ALIAS}

gemini.api.key=${GEMINI_API_KEY}
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent
jwt.refresh.expiration = ${REFRESH_EXPIRATION}

//frontend
frontend.url=${FRONTEND_URL:http://localhost:4200}
